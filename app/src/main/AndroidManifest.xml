<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wgout">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WGout">
        <activity android:name=".HomeActivity"> </activity>
        <activity android:name=".CalendarActivity"> </activity>
        <activity android:name=".CalendarScheduleActivity">  </activity>
        <activity android:name=".CalendarScheduleAddActivity">  </activity>
        <activity android:name=".CalendarScheduleShowActivity">  </activity>
        <activity android:name=".DestinationActivity">  </activity>
        <activity android:name=".DestinaitonAddActivity">  </activity>
        <activity android:name=".DestinationShowActivity">  </activity>
        <activity android:name=".LuckActivity">  </activity>

        <activity android:name=".WeatherActivity">

        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="q618nmd8vn" />
    </application>

</manifest>